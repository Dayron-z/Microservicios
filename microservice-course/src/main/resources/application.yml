server:
  port: 9090


spring:
  application:
    name: msvc-course
  datasource:
    driver-class-name: org.postgresql.Driver  # Corregido: Debe ser el nombre completo de la clase del driver JDBC
    url: jdbc:postgresql://localhost:5432/coursesDb   # Corregido: URL correcta de conexi?n a la base de datos MySQL
    username: postgre
    password : Rlwl2023.
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
